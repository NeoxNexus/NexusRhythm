# 项目教训库

> 🤖 此文件由 `/distill` 命令自动维护，每次会话启动时自动加载。
> 直接修改此文件需谨慎，优先使用 `/distill` 命令更新。

---

## 技术栈教训

> 当前为空。执行 `/distill` 后会从 Journal 和 Walkthrough 中自动提炼。

<!-- 示例格式：
- [库名] `decode()` 默认不验证 audience，必须显式设置 `algorithms=["HS256"]` + `audience="..."`
- [框架] 依赖注入不能在模块级别声明，必须作为函数参数使用
-->

---

## 架构教训

> 当前为空。

<!-- 示例格式：
- 不要在中间件中做数据库查询，用缓存层隔离
- 路由注册必须在所有中间件初始化之后进行
-->

---

## 工作流教训

> 当前为空。

<!-- 示例格式：
- 预估时间需要乘以 1.5 系数（历史数据：Phase 1-3 平均超时 47%）
- 集成第三方库前务必先写 spike test，一般需要额外 2-4 小时
-->

---

*最后蒸馏时间：——*
*蒸馏来源：——*
